<%= link_to("<< Back to List", {:action => 'index'}, :class => 'back-link') %>

<div class="woker edit">
  <h2>Edit worker</h2>
  <%= flash[:notice] %>
  </br>

  <%= form_for(:worker, :url => {:action => 'update', :id => @worker.id}) do |f| %>

    <%= render(:partial => "form", :locals => {:f => f}) %>

    <div class="form-buttons">
      <%= submit_tag("Add new worker") %>
    </div>

  <% end %>
</div>